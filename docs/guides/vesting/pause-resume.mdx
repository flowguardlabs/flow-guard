---
title: 'Pause and Resume'
description: 'Pause and resume supported stream families from the sender workflow.'
icon: 'circle-pause'
---

Pause and resume are sender controls for active streams. In the current product, pause is available on streams that were created as cancelable.

## Supported Families

- `LINEAR`
- `STEP`
- `HYBRID`
- `TRANCHE`
- `RECURRING`

## Who Can Pause or Resume

- only the **stream sender**
- only while the stream is in the correct status
  - `ACTIVE` to pause
  - `PAUSED` to resume

## Pause Flow

<Steps>
  <Step title="Open the stream">Navigate to the stream detail page as the sender.</Step>
  <Step title="Build the pause transaction">
    The app calls `POST /api/streams/:id/pause` with the sender address in `x-user-address`.
  </Step>
  <Step title="Sign and broadcast">Sign the returned transaction in the wallet.</Step>
  <Step title="Confirm the broadcast">
    The app calls `POST /api/streams/:id/confirm-pause` with the transaction hash. The stream status
    changes to `PAUSED`.
  </Step>
</Steps>

## Resume Flow

<Steps>
  <Step title="Build the resume transaction">
    Click **Resume** on a paused stream. The app calls `POST /api/streams/:id/resume`.
  </Step>
  <Step title="Sign and broadcast">Sign the returned wallet transaction.</Step>
  <Step title="Confirm the broadcast">
    The app calls `POST /api/streams/:id/confirm-resume`. The stream returns to `ACTIVE`.
  </Step>
</Steps>

## What Pause Means

- vesting-based schedules stop progressing while paused
- recurring schedules stop accruing new payment windows while paused
- no value is paid out by the pause transaction itself

## API Example

```json
POST /api/streams/:id/pause
x-user-address: bchtest:qsender...
```

```json
POST /api/streams/:id/confirm-pause
x-user-address: bchtest:qsender...

{
  "txHash": "hex64"
}
```
