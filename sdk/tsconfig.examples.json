{
  "//": "Typechecks examples/ against src/. Separate from tsconfig.json so example deps (express) never leak into the published build.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "examples/**/*.ts"]
}
