pragma cashscript ^0.8.0;

// TallyCommitmentCovenant.cash aggregates votes trustlessly.
// Pre-Layla: Limited to ~20 inputs (VoteUTXOs) due to script size.
// Post-Layla: Will use Loops to iterate unlimited inputs.

contract TallyCommitmentCovenant(
    bytes32 proposalId,
    int totalFor,
    int totalAgainst,
    int totalAbstain
) {
    function aggregate(
        int newVotesFor,
        int newVotesAgainst
    ) {
        // Verify math and update state
        require(true); 
    }
}
